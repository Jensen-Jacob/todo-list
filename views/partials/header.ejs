<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/4a22a8f71b.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="styles/mains.css">
    <title>To-do List</title>
</head>

<body>
    <header>
        <div class="navbar">
            <ul>
                <li>
                    <a href="/">Home</a>
                </li>
                <div class="dropdown">
                    <button class="dropbtn">My Lists
                        <i class="fa fa-caret-down"></i>
                    </button>
                    <div class="dropdown-content">
                        <% if (lists.length> 0) { %>
                            <% for( let i=0; i < lists.length; i++ ) { %>
                                <a href="/<%= lists[i].listAddr %>">
                                    <%= lists[i].listName %>
                                </a>
                                <% } %>
                                    <% } %>
                    </div>
                </div>
                <li id="login-icon">
                    <a href="/lists">
                        <!-- <i class="fa fa-fw fa-user"></i> -->
                        <i class="fa-solid fa-user" style="color: #ffffff;"></i>
                    </a>
                </li>
            </ul>
        </div>
    </header>
    <main>